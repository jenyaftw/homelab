apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: pangolin
  annotations:
    external-dns.alpha.kubernetes.io/target: pangolin.jenya.lol
    external-dns.alpha.kubernetes.io/external: 'true'
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
spec:
  endpoints:
    - dnsName: pangolin
      recordType: A
      targets: ["${VPS_IP}"]
