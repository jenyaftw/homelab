apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infra
resources:
  # Moved to infra-configs: DNSEndpoint requires external-dns CRDs
  # - dnsendpoint.yaml
  - secret.yaml
  - newt.yaml
