apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cert-manager.yaml
  - cloudflare-secret.yaml
  # Moved to infra-configs: ClusterIssuer requires CRDs to be installed first
  # - issuer.yaml
